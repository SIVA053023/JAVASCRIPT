class Account{
    constructor(a,b){             // a, b are the parameters
     //console,log("It is a special method")
     console.log(a,b)             // intialization
     
    }
}
let a1=new Account(10,20);      // 10, 20 are arguments or values 
  //  console.log(a1)

/* NOTES
  => Constructor is a special method to intialize theobject values
  => Constructor method will invoke automaticaly at the time of object creation only once
  =>we can't invoke the constructor method explicitly / more than once  */
  