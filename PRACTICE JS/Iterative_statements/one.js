let employees=[
]
let user= {id:103,ename:"Vamsi",sal:85000}

console.log(bject.)
/*for(emp of employees){
    rows=`(${emp.id}+${emp.ename}+${emp.sal})`
    console.log(rows)
}


console.log(employees[0])            //{ id: 101, ename: 'shiva', sal: 65000 }
console.log(Object.keys)*/
console.log(employees.length)

let data=(emp)=>{
    for(emp of employees){
        if(employees.length>1){
            console.log("not empty")
        }
        else{
            console.log("empty")
        }
    }
    
}
data()
