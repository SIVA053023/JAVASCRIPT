let a=459;
let b=599;
let c=699;

if(a>b && a>c){                                 // this is t he logic that applied to the given data
    console.log ("a is the greatest number")
}
else if(b>c){
    console.log("b is the greatest number")     // definitely here the ouput is "c is the greatest number" because the above two logics are incorect(if and if else);
}
else{
    console.log("c is the greatest number")
}