// implicit return
let wish=(a,b)=>a+b   // here we not using {} and the return also cause we just using one statement a+b so we can exclude
let r1=wish(20,30)  
let r2=wish(25,36)
console.log(r1);         // output=50
console.log(r2);        // output=61